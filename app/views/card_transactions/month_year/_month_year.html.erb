<div class="mb-8" data-datatable-target="table">
  <fieldset class="border border-slate-200 rounded-lg p-4 mb-4">
    <%= content_tag :legend, @month_year_str, class: "px-2 text-lg text-slate-800 text-start" %>

    <div class="bg-white rounded-lg border-1 border-slate-300 shadow-sm overflow-hidden">
      <div class="grid grid-cols-8 py-1 bg-slate-200 border-b border-slate-400 rounded-t-lg font-semibold text-black font-graduate">
        <%= content_tag :div, model_attribute(CardTransaction, :date), class: "py-3" %>
        <%= content_tag :div, model_attribute(CardTransaction, :description), class: "py-3 col-span-3" %>
        <%= content_tag :div, model_attribute(CardTransaction, :categories), class: "py-3" %>
        <%= content_tag :div, model_attribute(CardTransaction, :entities), class: "py-3" %>
        <%= content_tag :div, model_attribute(CardTransaction, :price), class: "py-3 text-end" %>
        <%= content_tag :div, t(:datatable_actions), class: "py-3" %>
      </div>

      <%= render @card_installments %>
    </div>
  </fieldset>
</div>
