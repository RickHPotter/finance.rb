<%= turbo_stream.update(:tabs, partial: "shared/tabs") %>

<%= turbo_stream.replace(:center_container, Views::UserBankAccounts::New.new(current_user:, user_bank_account: @user_bank_account, banks: @banks)) %>

<% if params[:no_user_bank_account] %>
  <%= turbo_stream.update(:notification, partial: "shared/flash", locals: { notice: notification_model(:user_bank_account_needed, CashTransaction) }) %>
<% end %>
