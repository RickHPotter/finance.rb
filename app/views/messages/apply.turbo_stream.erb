<% if params[:message_filter] == "pending" %>
  <%= turbo_stream.remove("message_entry_#{@message.id}") %>
<% else %>
  <%= turbo_stream.replace("message_entry_#{@message.id}", Views::Messages::Message.new(message: @message)) %>
<% end %>
