<%= turbo_stream.update(:tabs, partial: "shared/v1/tabs") %>

<%= turbo_stream.replace(:center_container, Views::V1::CardTransactions::Edit.new(current_user:, card_transaction: @card_transaction)) %>
