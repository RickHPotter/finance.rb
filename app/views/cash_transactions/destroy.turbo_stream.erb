<%= turbo_stream.update(
      :notification,
      partial: "shared/flash",
      locals: { notice: nil, alert: notification_model_or_history_lock(@cash_transaction, :not_destroyeda, CashTransaction) }
    ) %>
