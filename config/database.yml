default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV["POSTGRES_HOST"] || "127.0.0.1" %>
  port: <%= ENV["POSTGRES_PORT"] || 5432 %>
  username: <%= ENV["POSTGRES_USERNAME"] %>
  password: <%= ENV["POSTGRES_PASSWORD"] %>
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary: &primary_development
    <<: *default
    database: <%= ENV["POSTGRES_DATABASE"] %>_development
  queue:
    <<: *primary_development
    database: app_development_queue
    migrations_paths: db/queue_migrate
  cache:
    <<: *primary_development
    database: app_development_cache
    migrations_paths: db/cache_migrate
  cable:
    <<: *primary_development
    database: app_development_cable
    migrations_paths: db/cable_migrate

test:
  primary: &primary_test
    <<: *default
    database: <%= ENV["POSTGRES_DATABASE"] %>_test
  queue:
    <<: *primary_test
    database: app_test_queue
    migrations_paths: db/queue_migrate

production:
  primary: &primary_production
    <<: *default
    database: <%= ENV["POSTGRES_DATABASE"] %>
  queue:
    <<: *primary_production
    database: app_production_queue
    migrations_paths: db/queue_migrate
  cache:
    <<: *primary_production
    database: app_production_cache
    migrations_paths: db/cache_migrate
  cable:
    <<: *primary_production
    database: app_production_cable
    migrations_paths: db/cable_migrate

homolog:
  primary: &primary_homolog
    <<: *default
    database: <%= ENV["POSTGRES_DATABASE"] %>
  queue:
    <<: *primary_homolog
    database: app_homolog_queue
    migrations_paths: db/queue_migrate
  cache:
    <<: *primary_homolog
    database: app_homolog_cache
    migrations_paths: db/cache_migrate
  cable:
    <<: *primary_homolog
    database: app_homolog_cable
    migrations_paths: db/cable_migrate
