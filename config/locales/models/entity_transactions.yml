en:
  activerecord:
    models:
      entity_transaction:
        one: Entity Transaction
        other: Entity Transactions
    attributes:
      entity_transaction:
        id: ID
        transactable: Transactable
        transactable_id: Transactable
        transactable_type: Transactable
        entity: Entity
        entity_id: Entity
        is_payer: Payer
        price: Price
        price_to_be_returned: Price to be returned
        exchanges_count: Exchanges Count
        standalone: Standalone
        card_bound: Card Bound
        created_at: Created at
        updated_at: Updated at

pt-BR:
  activerecord:
    models:
      entity_transaction:
        one: Transação de Entidade
        other: Transações de Entidade
    attributes:
      entity_transaction:
        id: ID
        transactable: Transacionável
        transactable_id: ID da Transacionável
        transactable_type: Tipo da Transacionável
        entity: Entidade
        entity_id: ID da Entidade
        is_payer: Pagante
        price: Valor
        price_to_be_returned: Valor a ser devolvido
        exchanges_count: Quantidade de Trocas
        standalone: Simples
        card_bound: Cartão
        created_at: Criado em
        updated_at: Atualizado em
